# Library versioning
# Manual process to create a versioned library
# USBLIBVER variable is used to create the version number on the library
# The major number is incremented on an API change
# The minor number is incremented on a change
USBMAJOR = 1
USBLIBVER = $(USBMAJOR).1
